model:
  act: null
  hidden_layer: False
  dino_embed_dim: 768

train:
  lr: 0.0001
  ltype: 'infonce'
  num_epochs: 50
  batch_size: 128
  save_best_model: False

  lambda_obj: 240

  lambda_inst: 1.0
  lambda_overlap: 20.0
  lambda_spear: 40.0

  patch_temperature: 0.07
  em_iters: 1
  num_parts: 59

  epoch_part_mode: global_obj_bank
  max_patches_per_obj: 50000

  present_only_anchor: False

dataset:
  min_obj_area_ratio: 0.0
